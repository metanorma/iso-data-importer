# frozen_string_literal: true

RSpec.describe Iso::Data::Importer do
  it "has a version number" do
    expect(Iso::Data::Importer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
